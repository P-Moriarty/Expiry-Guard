// Use your machine's local IP address instead of 'localhost' 
// so the mobile app can reach the server when running on a real device or emulator.
export const API_IP = "192.168.8.253"; 
export const API_BASE_URL = `http://${API_IP}:3000/api`;
